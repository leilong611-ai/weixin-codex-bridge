{
  "name": "weixin-codex-bridge",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Standalone Weixin to Codex bridge with security and reliability guards",
  "engines": {
    "node": ">=22"
  },
  "main": "dist/cli.js",
  "files": [
    "dist/",
    "scripts/Install-CodexWeixinCompanionShortcut.ps1",
    "scripts/Launch-CodexWithWeixinBridge-Hidden.vbs",
    "scripts/Send-CodexDesktopInput.ps1",
    "scripts/Set-CodexDesktopModel.ps1",
    "scripts/Start-CodexWeixinBridge.ps1",
    "scripts/Start-CodexWithWeixinBridge.ps1",
    "scripts/Status-CodexWeixinBridge.ps1",
    "scripts/Test-CodexWeixinSetup.ps1",
    "scripts/Watch-CodexWeixinBridge.ps1",
    "README.md",
    "README.en.md",
    "LICENSE",
    "SECURITY.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "prestart": "npm run build",
    "test": "vitest",
    "login": "node dist/cli.js",
    "doctor": "node dist/cli.js",
    "setup-check": "powershell -ExecutionPolicy Bypass -File scripts/Test-CodexWeixinSetup.ps1",
    "public-check": "node -e \"const cp=require('node:child_process'); const candidates=process.platform==='win32'?[process.env.GIT_BASH,'C:/Program Files/Git/bin/bash.exe','C:/Program Files (x86)/Git/bin/bash.exe','bash'].filter(Boolean):['bash']; for (const cmd of candidates){const r=cp.spawnSync(cmd,['./scripts/public-check.sh'],{stdio:'inherit'}); if(!r.error) process.exit(r.status ?? 0); if(r.error.code!=='ENOENT'){console.error(r.error.message); process.exit(1);}} console.error('Unable to find bash. Install Git Bash or run scripts/public-check.sh from a bash shell.'); process.exit(127);\"",
    "prepublishOnly": "npm run build && npm test -- --run && npm run public-check",
    "pack:verify": "npm pack --dry-run --json 2>&1"
  },
  "dependencies": {
    "acpx": "0.3.1",
    "qrcode": "1.5.4",
    "qrcode-terminal": "0.12.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}